# 4.5 IMPLEMENTASI SISTEM

Implementasi sistem merupakan tahapan penerjemahan perancangan sistem ke dalam kode program yang dapat dijalankan. Pada tahap ini, seluruh desain yang telah dibuat pada sub-bab sebelumnya (diagram UML, struktur database, dan rancangan antarmuka) diimplementasikan menggunakan teknologi yang telah ditentukan.

Sistem POS DW diimplementasikan menggunakan framework Laravel 13 dengan bahasa pemrograman PHP 8.3. Untuk frontend, digunakan Livewire 4 yang memungkinkan pembuatan antarmuka reaktif tanpa perlu menulis kode JavaScript secara eksplisit.

---

## 4.5.1 Implementasi Database

Implementasi database dilakukan menggunakan Laravel Migration, yaitu fitur version control untuk database yang memungkinkan pengelolaan struktur tabel secara terprogram. Seluruh migration dijalankan menggunakan perintah `php artisan migrate` yang akan membuat tabel-tabel di database MySQL.

### Daftar Migration

**Tabel 4.24 Daftar File Migration**

| No | Nama File | Tabel | Fungsi | Tanggal |
|----|-----------|-------|--------|---------|
| 1 | 0001_01_01_000000_create_users_table.php | users | Membuat tabel users dengan kolom autentikasi | Default Laravel |
| 2 | 0001_01_01_000001_create_cache_table.php | cache, cache_locks | Membuat tabel untuk Laravel cache | Default Laravel |
| 3 | 0001_01_01_000002_create_jobs_table.php | jobs, job_batches, failed_jobs | Membuat tabel untuk queue dan job | Default Laravel |
| 4 | 2025_05_09_000001_create_categories_table.php | categories | Membuat tabel kategori produk | 09-05-2025 |
| 5 | 2025_05_09_000002_create_products_table.php | products | Membuat tabel produk dengan foreign key | 09-05-2025 |
| 6 | 2025_05_09_000003_create_transactions_table.php | transactions | Membuat tabel header transaksi | 09-05-2025 |
| 7 | 2025_05_09_000004_create_transaction_items_table.php | transaction_items | Membuat tabel detail item transaksi | 09-05-2025 |
| 8 | 2025_08_14_170933_add_two_factor_columns_to_users_table.php | users | Menambah kolom 2FA ke tabel users | 14-08-2025 |
| 9 | 2026_05_11_230521_create_permission_tables.php | roles, permissions, model_has_roles, role_has_permissions | Membuat tabel untuk Spatie Permission | 11-05-2026 |
| 10 | 2026_05_11_231047_create_settings_table.php | settings | Membuat tabel konfigurasi sistem | 11-05-2026 |
| 11 | 2026_05_14_013457_add_is_unlimited_stock_to_products_table.php | products | Menambah kolom is_unlimited_stock | 14-05-2026 |
| 12 | 2026_05_14_014005_add_image_to_products_table.php | products | Menambah kolom image untuk gambar produk | 14-05-2026 |
| 13 | 2026_06_15_181423_create_sessions_table.php | sessions | Membuat tabel session database | 15-06-2026 |

*Sumber: Hasil Implementasi (2026)*

### Contoh Kode Migration

Berikut adalah contoh implementasi migration untuk tabel products:

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('products', function (Blueprint $table) {
            $table->id();
            $table->foreignId('category_id')->constrained()->cascadeOnDelete();
            $table->string('name', 200);
            $table->string('slug', 220)->unique();
            $table->string('sku', 50)->unique()->index();
            $table->decimal('price', 12, 2)->default(0);
            $table->integer('stock')->default(0);
            $table->text('description')->nullable();
            $table->boolean('is_active')->default(true)->index();
            $table->timestamps();

            $table->index('category_id');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('products');
    }
};
```

(Sumber: File `database/migrations/2025_05_09_000002_create_products_table.php`)

### Eksekusi Migration

Migration dijalankan menggunakan perintah Artisan:

```bash
php artisan migrate
```

Perintah ini akan mengeksekusi seluruh migration yang belum dijalankan secara berurutan. Setiap migration dicatat dalam tabel migrations sehingga hanya migration baru yang akan dieksekusi pada eksekusi berikutnya.

### Status Migration

Setelah seluruh migration dijalankan, status dapat diverifikasi dengan perintah:

```bash
php artisan migrate:status
```

Hasil eksekusi menunjukkan seluruh 13 migration telah berhasil dijalankan (status "Ran").

---

## 4.5.2 Implementasi Program

### Teknologi yang Digunakan

**Tabel 4.25 Stack Teknologi Implementasi**

| Layer | Teknologi | Versi | Fungsi |
|-------|-----------|-------|--------|
| **Bahasa Pemrograman** | PHP | 8.3.31 | Bahasa utama backend |
| **Framework Backend** | Laravel | 13.9.0 | Framework MVC utama |
| **Frontend Reactive** | Livewire | 4.3.0 | Komponen frontend reaktif |
| **UI Components** | Livewire Flux | 2.13.1 | Komponen antarmuka siap pakai |
| **CSS Framework** | Tailwind CSS | 4.0+ | Styling utility-first |
| **Page Routing** | Laravel Folio | 1.1+ | Routing berbasis halaman |
| **Authentication** | Laravel Fortify | 1.34+ | Backend autentikasi + 2FA |
| **Authorization** | Spatie Permission | 7.4.1 | Role & permission management |
| **Database** | MySQL | 8.0+ | Database relasional |
| **Asset Builder** | Vite | 5.0+ | Build tool frontend |

### Struktur Folder Aplikasi

```
app/
├── Livewire/Actions/
│   └── Logout.php              # Livewire component logout
├── Models/
│   ├── User.php                 # Model pengguna
│   ├── Category.php             # Model kategori produk
│   ├── Product.php              # Model produk
│   ├── Transaction.php          # Model transaksi
│   ├── TransactionItem.php      # Model item transaksi
│   └── Setting.php              # Model pengaturan
└── Providers/
    └── AppServiceProvider.php   # Service provider

database/
├── factories/
│   ├── UserFactory.php          # Factory untuk testing
│   ├── CategoryFactory.php
│   ├── ProductFactory.php
│   └── TransactionFactory.php
├── migrations/                  # 13 file migration
└── seeders/
    └── DatabaseSeeder.php       # Seeder database

resources/views/
├── pages/                       # Laravel Folio pages
│   ├── auth/                    # Halaman autentikasi (6 file)
│   ├── categories/              # Halaman kategori
│   ├── products/                # Halaman produk (3 file)
│   ├── transactions/            # Halaman transaksi (3 file)
│   ├── reports/                 # Halaman laporan
│   ├── users/                   # Halaman user (3 file)
│   ├── roles/                   # Halaman role (3 file)
│   ├── permissions/             # Halaman permission (3 file)
│   └── settings/                # Halaman settings (8 file)
├── layouts/                     # Layout template
└── components/                  # Komponen reusable

routes/
└── web.php                      # Route definitions
```

### Implementasi Eloquent Models

#### Model Category

Model Category merepresentasikan entitas kategori produk dengan relasi one-to-many ke Product.

```php
<?php

namespace App\Models;

use Database\Factories\CategoryFactory;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\HasMany;

class Category extends Model
{
    /** @use HasFactory<CategoryFactory> */
    use HasFactory;

    protected $fillable = ['name', 'slug', 'description'];

    public function products(): HasMany
    {
        return $this->hasMany(Product::class);
    }
}
```

(Sumber: File `app/Models/Category.php`)

#### Model Product

Model Product merupakan model inti yang memiliki relasi ke Category dan TransactionItem. Model ini juga memiliki accessor untuk mengelola URL gambar produk.

```php
<?php

namespace App\Models;

use Database\Factories\ProductFactory;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;
use Illuminate\Database\Eloquent\Relations\HasMany;

class Product extends Model
{
    /** @use HasFactory<ProductFactory> */
    use HasFactory;

    protected $fillable = [
        'category_id', 'name', 'slug', 'sku',
        'price', 'stock', 'image', 'description', 'is_active',
        'is_unlimited_stock',
    ];

    protected function casts(): array
    {
        return [
            'is_active' => 'boolean',
            'is_unlimited_stock' => 'boolean',
            'price' => 'float',
        ];
    }

    public function getImageUrlAttribute(): string
    {
        if (! $this->image) {
            return asset('images/product-default.svg');
        }

        if (str_starts_with($this->image, 'http')) {
            return $this->image;
        }

        if (file_exists(storage_path('app/public/'.$this->image))) {
            return asset('storage/'.$this->image);
        }

        return asset('images/product-default.svg');
    }

    public function category(): BelongsTo
    {
        return $this->belongsTo(Category::class);
    }

    public function transactionItems(): HasMany
    {
        return $this->hasMany(TransactionItem::class);
    }
}
```

(Sumber: File `app/Models/Product.php`)

#### Model User

Model User mengimplementasikan autentikasi menggunakan Laravel Fortify dan role-based access control menggunakan Spatie Permission.

```php
<?php

namespace App\Models;

use Database\Factories\UserFactory;
use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Hidden;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Relations\HasMany;
use Illuminate\Foundation\Auth\User as Authenticatable;
use Illuminate\Notifications\Notifiable;
use Illuminate\Support\Str;
use Laravel\Fortify\TwoFactorAuthenticatable;

#[Fillable(['name', 'email', 'password'])]
#[Hidden(['password', 'two_factor_secret', 'two_factor_recovery_codes', 'remember_token'])]
class User extends Authenticatable
{
    /** @use HasFactory<UserFactory> */
    use HasFactory, Notifiable, \Spatie\Permission\Traits\HasRoles, TwoFactorAuthenticatable;

    protected function casts(): array
    {
        return [
            'email_verified_at' => 'datetime',
            'password' => 'hashed',
        ];
    }

    public function initials(): string
    {
        return Str::of($this->name)
            ->explode(' ')
            ->take(2)
            ->map(fn ($word) => Str::substr($word, 0, 1))
            ->implode('');
    }

    public function transactions(): HasMany
    {
        return $this->hasMany(Transaction::class);
    }
}
```

(Sumber: File `app/Models/User.php`)

### Routing (Laravel Folio)

Sistem menggunakan Laravel Folio untuk routing berbasis halaman. Dengan Folio, setiap file Blade di folder `resources/views/pages/` secara otomatis menjadi route berdasarkan path file tersebut.

**Tabel 4.26 Mapping Route dengan Halaman**

| URL Path | File Blade | Nama Route | Middleware |
|----------|-----------|------------|------------|
| `/` | `resources/views/welcome.blade.php` | home | - |
| `/dashboard` | `resources/views/dashboard.blade.php` | dashboard | auth, verified |
| `/login` | `resources/views/pages/auth/login.blade.php` | login | guest |
| `/register` | `resources/views/pages/auth/register.blade.php` | register | guest |
| `/categories` | `resources/views/pages/categories/index.blade.php` | categories.index | auth, verified |
| `/products` | `resources/views/pages/products/index.blade.php` | products.index | auth, verified |
| `/products/create` | `resources/views/pages/products/create.blade.php` | products.create | auth, verified |
| `/products/{product}` | `resources/views/pages/products/[Product].blade.php` | products.show | auth, verified |
| `/transactions` | `resources/views/pages/transactions/index.blade.php` | transactions.index | auth, verified |
| `/transactions/create` | `resources/views/pages/transactions/create.blade.php` | transactions.create | auth, verified |
| `/transactions/{transaction}` | `resources/views/pages/transactions/[Transaction].blade.php` | transactions.show | auth, verified |
| `/transactions/{transaction}/receipt` | `resources/views/pages/transactions/[Transaction]/receipt.blade.php` | transactions.receipt | auth, verified |
| `/reports` | `resources/views/pages/reports/index.blade.php` | reports.index | auth, verified |
| `/users` | `resources/views/pages/users/index.blade.php` | users.index | auth, verified |
| `/roles` | `resources/views/pages/roles/index.blade.php` | roles.index | auth, verified |
| `/settings/profile` | `resources/views/pages/settings/profile.blade.php` | settings.profile | auth |
| `/settings/security` | `resources/views/pages/settings/security.blade.php` | settings.security | auth |
| `/settings/store` | `resources/views/pages/settings/store.blade.php` | settings.store | auth |
| `/settings/appearance` | `resources/views/pages/settings/appearance.blade.php` | settings.appearance | auth |

### Implementasi Livewire Component (Volt)

Sistem menggunakan Livewire Volt, yaitu single-file component yang menggabungkan logika PHP dan template Blade dalam satu file. Berikut adalah contoh implementasi halaman transaksi POS yang menunjukkan fitur real-time:

**State Management (Halaman Transaksi POS):**

Halaman transaksi POS menggunakan state management dari Livewire untuk mengelola data keranjang secara real-time:

```php
<?php

use App\Models\Category;
use App\Models\Product;
use App\Models\Transaction;
use App\Models\TransactionItem;
use Flux\Flux;
use Illuminate\Support\Str;
use Livewire\WithPagination;

use function Laravel\Folio\middleware;
use function Laravel\Folio\name;
use function Livewire\Volt\computed;
use function Livewire\Volt\state;
use function Livewire\Volt\usesPagination;

usesPagination();

name('transactions.create');
middleware('auth');
middleware('verified');

state([
    'customer' => '',
    'category_id' => (int) '',
    'productSearch' => '',
    'cart' => [],
    'payment_method' => 'cash',
    'paid_amount' => 0,
    'notes' => '',
    'showConfirmModal' => false,
]);
```

(Sumber: File `resources/views/pages/transactions/create.blade.php`)

**Computed Properties untuk Data Real-Time:**

```php
$categoryOptions = computed(function () {
    return Category::orderBy('name')->get();
});

$products = computed(function () {
    $query = Product::query()->where('is_active', true);

    if (!empty($this->category_id)) {
        $query->where('category_id', $this->category_id);
    }

    if (!empty($this->productSearch)) {
        $query->where(function ($q) {
            $q->where('name', 'like', '%' . $this->productSearch . '%')
              ->orWhere('sku', 'like', '%' . $this->productSearch . '%');
        });
    }

    return $query->orderBy('name')->paginate(12);
});

$totalAmount = computed(function () {
    return collect($this->cart)->sum('subtotal');
});
```

**Action untuk Menambahkan ke Keranjang:**

```php
$addToCart = function ($productId) {
    $product = Product::findOrFail($productId);

    if (isset($this->cart[$productId])) {
        $this->cart[$productId]['quantity']++;
    } else {
        $this->cart[$productId] = [
            'product_id' => $productId,
            'name' => $product->name,
            'price' => $product->price,
            'quantity' => 1,
            'subtotal' => $product->price,
        ];
    }

    $this->cart[$productId]['subtotal'] =
        $this->cart[$productId]['quantity'] * $this->cart[$productId]['price'];
};
```

(Sumber: File `resources/views/pages/transactions/create.blade.php`)

### Autentikasi dan Keamanan

Sistem menggunakan Laravel Fortify untuk backend autentikasi yang mencakup:

1. **Login**: Menggunakan email dan password dengan validasi dan throttling
2. **Two-Factor Authentication (2FA)**: Menggunakan Google Authenticator dengan QR Code setup
3. **Email Verification**: Verifikasi alamat email untuk mengaktifkan akun
4. **Password Reset**: Fitur lupa password dengan email token
5. **Password Confirmation**: Konfirmasi password ulang untuk aksi sensitif

Untuk otorisasi, sistem menggunakan Spatie Laravel Permission dengan implementasi Role-Based Access Control (RBAC):

```php
// Contoh pengecekan permission di blade
@can('view-products')
    <a href="/products">Kelola Produk</a>
@endcan

// Contoh pengecekan role
@role('admin')
    <a href="/users">Kelola Pengguna</a>
@endrole
```

---

## 4.5.3 Implementasi Tampilan

Berikut adalah hasil implementasi tampilan sistem POS DW yang telah dikembangkan berdasarkan perancangan antarmuka pada sub-bab sebelumnya. Setiap halaman diimplementasikan dengan memperhatikan aspek usability, responsivitas, dan konsistensi desain.

### Halaman Login

**Gambar 4.25 Tampilan Halaman Login**

![UI Login](bab-iv/images/ui-login.png)

*Sumber: Dokumentasi Implementasi (2026)*

Halaman login merupakan implementasi dari rancangan yang telah dibuat sebelumnya. Halaman ini menampilkan form autentikasi dengan input email dan password. Tampilan menggunakan desain minimalis dengan posisi form di tengah layar (centered). Background menggunakan warna gradasi yang memberikan kesan profesional.

**Fitur yang telah diimplementasikan:**
- Validasi input sisi client menggunakan Alpine.js
- Error message yang informatif untuk setiap kesalahan input
- Loading spinner saat proses autentikasi
- Integrasi dengan Laravel Fortify untuk keamanan

### Halaman Dashboard

**Gambar 4.26 Tampilan Halaman Dashboard**

![UI Dashboard](bab-iv/images/ui-dashboard.png)

*Sumber: Dokumentasi Implementasi (2026)*

Dashboard menampilkan ringkasan informasi bisnis dalam bentuk card statistik dan grafik. Layout terdiri dari sidebar navigasi di kiri, header di atas, dan konten utama di tengah.

**Komponen yang telah diimplementasikan:**
- **Sidebar Navigasi**: Menu navigasi dengan ikon yang responsif (bisa disembunyikan di mobile)
- **Card Statistik**: Total penjualan, jumlah transaksi, dan stok menipis dengan ikon dan warna berbeda
- **Grafik Penjualan**: Visualisasi data penjualan menggunakan grafik

### Halaman Daftar Produk

**Gambar 4.27 Tampilan Halaman Produk**

![UI Products](bab-iv/images/ui-products.png)

*Sumber: Dokumentasi Implementasi (2026)*

Halaman produk menampilkan daftar produk dalam bentuk grid card yang informatif. Setiap card menampilkan gambar produk, nama, SKU, harga, dan stok.

**Fitur yang telah diimplementasikan:**
- Pencarian real-time: Hasil pencarian berubah saat user mengetik (wire:model.live)
- Filter kategori: Dropdown filter untuk memfilter produk per kategori
- Pagination: Navigasi halaman dengan informasi jumlah data
- Status badge: Indikator visual untuk status aktif dan stok menipis

### Halaman Form Produk

**Gambar 4.28 Tampilan Form Produk**

![UI Product Form](bab-iv/images/ui-product-form.png)

*Sumber: Dokumentasi Implementasi (2026)*

Form produk menyediakan input lengkap untuk data produk dengan validasi di sisi client dan server.

**Fitur validasi:**
- Nama produk: required, max 200 karakter
- SKU: required, unique, auto-format
- Harga: required, numerik, format Rupiah
- Gambar: validasi tipe file (JPG, PNG, WEBP) dan ukuran (max 2MB)
- Preview gambar sebelum upload

### Halaman Transaksi POS

**Gambar 4.29 Tampilan Halaman Transaksi POS**

![UI Transactions POS](bab-iv/images/ui-transactions.png)

*Sumber: Dokumentasi Implementasi (2026)*

Halaman transaksi POS merupakan halaman paling kompleks dalam sistem. Tampilan menggunakan layout dua panel yang optimal untuk produktivitas kasir.

**Fitur yang telah diimplementasikan:**
- Pencarian produk real-time
- Keranjang belanja dengan update quantity
- Perhitungan subtotal dan total otomatis
- Perhitungan kembalian real-time
- Multiple payment method (Cash, Debit Card, Credit Card, E-Wallet)
- Database transaction untuk atomicity

### Halaman Laporan

**Gambar 4.30 Tampilan Halaman Laporan**

![UI Reports](bab-iv/images/ui-reports.png)

*Sumber: Dokumentasi Implementasi (2026)*

Halaman laporan menyediakan interface untuk generate dan export laporan bisnis.

**Fitur yang telah diimplementasikan:**
- Tiga jenis laporan: Penjualan, Stok, Produk Terlaris
- Date range picker untuk filter periode
- Export ke PDF dan Excel
- Tampilan tabel dengan sorting

### Halaman Pengaturan

**Gambar 4.31 Tampilan Halaman Settings Toko**

![UI Store Settings](bab-iv/images/ui-settings-store.png)

*Sumber: Dokumentasi Implementasi (2026)*

Halaman pengaturan menyediakan konfigurasi untuk data toko, profil user, dan keamanan.

**Fitur yang telah diimplementasikan:**
- Settings toko (nama, alamat, telepon, logo)
- Profile settings (nama, email)
- Security settings (password, 2FA, active sessions)
- Upload logo toko

---

## Ringkasan Implementasi

Implementasi Sistem POS DW telah berhasil dilakukan dengan hasil sebagai berikut:

1. **Database**: 13 tabel berhasil dibuat dan dijalankan melalui Laravel Migration
2. **Backend**: Implementasi menggunakan Laravel 13 dengan 6 model Eloquent, pattern MVC, dan repository pattern
3. **Frontend**: Implementasi menggunakan Livewire 4 + Volt dengan komponen real-time tanpa reload
4. **Autentikasi**: Laravel Fortify dengan fitur 2FA, email verification, dan password reset
5. **Otorisasi**: Spatie Permission dengan role-based access control (Admin, Admin)
6. **Routing**: Laravel Folio dengan page-based routing untuk kemudahan navigasi
7. **UI Components**: Livewire Flux UI dengan Tailwind CSS 4 untuk tampilan yang modern dan responsif

Seluruh implementasi dilakukan dengan memperhatikan best practices pengembangan Laravel modern dan siap untuk memasuki tahap pengujian sistem.

---

## Navigasi

| [← Daftar Isi](./README.md) | [← 4.4 Perancangan Antarmuka](./4.4-perancangan-antarmuka.md) | [4.6 Pengujian Sistem →](./4.6-pengujian-sistem.md) |
|:---:|:---:|:---:|
